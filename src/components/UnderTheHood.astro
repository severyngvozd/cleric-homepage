---
import img1 from '../assets/image/under-the-hood/enviroments_img1.png';
import img2 from '../assets/image/under-the-hood/enviroments_img2.png';
import img3 from '../assets/image/under-the-hood/enviroments_img3.png';
---

<section id="under-the-hood" class="under-the-hood">
	<div class="under-the-hood-container">
		<div class="pill">Under the hood</div>
		<h2 class="section-title">Simple on the surface. Serious underneath.</h2>
		<p class="section-description">
			Three purpose-built systems that turn alerts into answers—and make every investigation faster than the last.
		</p>

		<div class="top-images">
			<div class="image-block">
				<h3 class="feature-title">Maps your world</h3>
				<p class="feature-description">
					Services, dependencies, and ownership mapped automatically. When something breaks, Cleric knows where to look.
				</p>
				<div class="image-card">
					<img src={img1.src} alt="Maps your world" class="feature-image" />
				</div>
			</div>

			<div class="image-block">
				<h3 class="feature-title">Tests every theory</h3>
				<p class="feature-description">
					Correlates signals, tests hypotheses in parallel, tracks confidence. Systematic elimination, not guesswork.
				</p>
				<div class="image-card">
					<img src={img2.src} alt="Tests every theory" class="feature-image" />
				</div>
			</div>
		</div>

		<div class="bottom-image">
			<div class="image-block">
				<h3 class="feature-title">Compounds over time</h3>
				<p class="feature-description">
					Every fix teaches Cleric something new. Patterns and context accumulate automatically.
				</p>
				<div class="image-card">
					<img src={img3.src} alt="Compounds over time" class="feature-image" />
				</div>
			</div>
		</div>

		<div class="cta-container">
			<button class="cta-button">Explore Full Platform</button>
		</div>
	</div>
</section>

<style>
	.under-the-hood {
        padding: 80px 0 160px 0;
		background: #ffffff;
	}

	.under-the-hood-container {
        max-width: 1192px;
		margin: 0 auto;
		text-align: center;
	}

	.pill {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 8px 16px;
		border-radius: 4px;
		background-color: #e0e0e0;
		color: #6b6b6b;
		font-size: 14px;
		font-weight: 500;
		margin-bottom: 24px;
	}

	.section-title {
		font-size: 48px;
		font-weight: 400;
		color: #313131;
		margin: 0 0 8px;
	}

	.section-description {
		font-size: 18px;
		color: #6b6b6b;
		max-width: 720px;
		margin: 0 auto 64px;
	}

	.top-images {
		display: flex;
		gap: 24px;
		margin-bottom: 24px;
	}

	.bottom-image {
		margin-bottom: 40px;
	}

	.image-block {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		text-align: center;
        justify-content: space-between;
        background: #F7F9FA;
        border-radius: 16px;
        border: 1px solid #EAEAEA;
        padding: 48px 48px 16px 48px;
		flex: 1;
	}

	.image-card {
		width: 100%;
		display: flex;
		justify-content: center;
		align-items: center;
		position: relative;
		overflow: hidden;
	}

	.feature-image {
		display: block;
		width: 100%;
		max-width: 100%;
		height: auto;
		object-fit: contain;
	}

	.top-images .image-block:nth-child(2) .image-card::after {
		content: '';
		position: absolute;
		top: 0;
		right: 0;
		width: 120px;
		height: 100%;
		pointer-events: none;
		background: linear-gradient(270deg, #f8f8f8 0%, rgba(248, 248, 248, 0) 100%);
	}

	.feature-title {
		font-size: 32px;
		font-weight: 400;
		color: #313131;
		margin: 0 0 16px;
	}

	.feature-description {
		font-size: 18px;
		color: #313131;
		margin: 0 0 24px;
		max-width: 100%;
		text-align: left;
		line-height: 1.4;
        max-width: 512px;
	}

	.bottom-image .image-block {
		margin: 0 auto;
		height: 480px;
        overflow: hidden;
	}

	.bottom-image .image-card {
		margin-left: -48px;
		margin-right: -48px;
        margin-bottom: -20px;
		width: calc(100% + 96px);
	}

	.cta-container {
		display: flex;
		justify-content: center;
		
	}

	.cta-button {
		padding: 10px 24px;
		border-radius: 999px;
		background: #000000;
		color: #ffffff;
		font-size: 15px;
		font-weight: 500;
		border: none;
		cursor: pointer;
		transition: background 0.3s;
	}

	.cta-button:hover {
		background: rgba(0, 0, 0, 0.8);
	}

	@media (max-width: 991px) {
        .under-the-hood {
            padding: 60px 96px 60px 96px;
        }

		.under-the-hood-container {
			text-align: left;
		}

		.section-title {
			font-size: 40px;
		}

		.section-description {
			font-size: 16px;
		}

		.pill {
			font-size: 13px;
		}

		.top-images {
			flex-direction: column;
			gap: 24px;
		}

		.image-block {
			padding: 32px 32px 16px 32px;
		}

		.bottom-image .image-block {
			padding: 32px 32px 16px 32px;
			justify-content: flex-start;
			height: auto;
		}
	}

	@media (max-width: 767px) {
		.under-the-hood {
			padding: 40px 16px 40px 16px;
		}

		.under-the-hood-container {
			padding: 0;
			text-align: left;
		}

		.section-title {
			font-size: 32px;
		}

		.section-description {
			font-size: 16px;
		}

		.pill {
			font-size: 12px;
		}

		.feature-title {
			font-size: 24px;
		}

		.feature-description {
			font-size: 16px;
		}

		.top-images {
			flex-direction: column;
			gap: 24px;
		}

		.image-block {
			padding: 24px 24px 16px 24px;
		}

		.bottom-image .image-block {
			padding: 24px 24px 16px 24px;
			justify-content: flex-start;
			height: auto;
		}
	}
</style>

